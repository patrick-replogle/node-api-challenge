import React from "react";
import axios from "axios";

const Form = ({
  isEditing,
  setIsEditing,
  projectList,
  setProjectList,
  itemToEdit,
  setItemToEdit
}) => {
  return (
    <>
      <h2>Add a Project</h2>
      <form>
        <input />
        <input />
      </form>
    </>
  );
};

export default Form;
